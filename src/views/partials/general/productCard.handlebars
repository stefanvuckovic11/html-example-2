<div class="product-card">
    <div class="product-card__img-wrapper">
        <img src="{{imgSrc}}" alt="{{altText}}" class="product-card__img">
    </div>
    <h3 class="product-card__title">{{title}}</h3>
    <p class="product-card__description">{{description}}</p>
</div>
